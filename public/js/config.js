// public/js/config.js
//
// API base URL. Update this if the tunnel domain changes (it won't with ngrok static domains).
// For local dev: change to 'http://127.0.0.1:3000'

window.ATLAS_API_URL = 'https://alva-keyed-unexplainably.ngrok-free.dev';
